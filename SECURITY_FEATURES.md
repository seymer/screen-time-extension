# 安全保护功能说明

## 概述

为了防止用户在时间用完后修改或绕过限制,我们添加了多层安全保护机制。

## 主要功能

### 1. 密码保护 🔐

- **设置密码**: 在设置页面的"Security & Protection"部分可以设置安全密码
- **密码要求**: 最少4个字符
- **保护范围**: 
  - 修改网站限制
  - 删除网站限制
  - 禁用追踪功能
  - 清除所有数据

### 2. 操作冷却期 ⏱️

- **默认冷却期**: 1小时
- **可配置**: 30分钟 - 8小时
- **作用**: 防止频繁修改限制来绕过时间控制
- **示例**: 如果用户在下午2点修改了YouTube的限制,则需要等到下午3点才能再次修改

### 3. 每日修改次数限制 📊

- **默认限制**: 每天最多3次
- **可配置**: 1-10次/天
- **作用**: 防止通过多次小幅度修改来累积更多时间
- **重置**: 每天午夜自动重置

### 4. 审计日志 📝

- **记录内容**:
  - 所有限制的修改和删除
  - 密码的设置和移除
  - 追踪功能的启用/禁用
  - 数据清除操作
  - 失败的认证尝试
- **查看方式**: 设置页面 → Security & Protection → View Audit Log
- **保留数量**: 最近100条记录

### 5. 数据完整性验证 🛡️

- **校验和保护**: 使用SHA-256哈希验证限制数据未被篡改
- **篡改检测**: 如果检测到数据被修改,会记录到审计日志
- **自动修复**: 未来版本可能会添加自动恢复功能

## 使用指南

### 首次设置

1. 打开扩展设置页面
2. 找到"Security & Protection"部分
3. 点击"Set Password"设置安全密码
4. 启用"Enable Security Protection"开关
5. 配置具体的保护选项:
   - ✅ Require Password for Limit Changes (推荐)
   - ✅ Require Password to Disable Tracking (推荐)
   - ✅ Require Password to Clear Data (推荐)
   - 设置冷却期(推荐1-2小时)
   - 设置每日最大修改次数(推荐2-3次)

### 修改限制时的流程

1. 尝试修改网站限制
2. 系统检查:
   - ✓ 是否在冷却期内?
   - ✓ 今天是否已达到修改次数上限?
   - ✓ 是否需要密码验证?
3. 如果需要密码,会弹出输入框
4. 输入正确密码后才能保存修改
5. 修改会被记录到审计日志

### 禁用追踪时的流程

1. 在popup中尝试关闭"Tracking enabled"开关
2. 如果启用了密码保护,会提示输入密码
3. 输入正确密码后才能禁用追踪
4. 操作会被记录到审计日志

### 查看审计日志

1. 打开设置页面
2. 找到"Security & Protection"部分
3. 点击"View Audit Log"按钮
4. 查看所有安全相关操作的历史记录

## 技术实现

### 密码存储

- 使用SHA-256哈希算法
- 只存储哈希值,不存储明文密码
- 存储位置: Chrome本地存储(chrome.storage.local)

### 安全检查流程

```javascript
// 1. 检查是否启用安全保护
if (securitySettings.enabled && isPasswordSet) {
  
  // 2. 检查操作锁定状态
  if (isLocked()) {
    return "Settings are locked";
  }
  
  // 3. 检查修改冷却期
  if (withinCooldownPeriod()) {
    return "Please wait before modifying again";
  }
  
  // 4. 检查每日修改次数
  if (exceededDailyLimit()) {
    return "Maximum modifications reached today";
  }
  
  // 5. 验证密码
  if (requiresPassword && !verifyPassword(password)) {
    return "Invalid password";
  }
  
  // 6. 允许操作并记录
  recordModification();
  logAudit();
}
```

### 数据结构

```javascript
// 安全设置
{
  enabled: boolean,
  requirePasswordForLimitChanges: boolean,
  requirePasswordForDisableTracking: boolean,
  requirePasswordForDataClear: boolean,
  cooldownPeriod: number, // 秒
  maxModificationsPerDay: number
}

// 修改记录
{
  "2026-02-03": [
    {
      type: "MODIFY_LIMIT",
      domain: "youtube.com",
      timestamp: 1738588800000
    }
  ]
}

// 审计日志
[
  {
    action: "MODIFY_LIMIT",
    description: "Limit modified for youtube.com",
    metadata: {...},
    timestamp: 1738588800000,
    date: "2026-02-03T12:00:00.000Z"
  }
]
```

## 安全建议

### 对于家长

1. **设置强密码**: 使用至少8个字符,包含字母和数字
2. **不要分享密码**: 不要告诉孩子密码
3. **定期检查日志**: 每周查看一次审计日志,了解是否有异常操作
4. **合理设置冷却期**: 建议设置2-4小时,既能防止绕过又不会太严格
5. **限制修改次数**: 建议每天2-3次,应对合理的调整需求

### 对于个人使用

1. **设置密码**: 即使是自己使用,设置密码也能增加"摩擦力"
2. **适度冷却期**: 1-2小时的冷却期可以让你三思而后行
3. **查看日志**: 定期查看自己的使用模式,了解自己的习惯

## 限制和注意事项

### 当前限制

1. **浏览器级别**: 保护仅在Chrome浏览器内有效
2. **本地存储**: 数据存储在本地,技术熟练的用户可能通过开发者工具绕过
3. **扩展卸载**: 用户仍然可以卸载扩展(未来版本可能添加企业策略支持)

### 最佳实践

1. **配合其他措施**: 这个功能应该作为自律工具的一部分,而不是唯一的控制手段
2. **定期沟通**: 对于家长控制场景,应该与孩子保持沟通
3. **合理限制**: 不要设置过于严格的限制,否则可能适得其反

## 故障排除

### 忘记密码

如果忘记密码,目前有两种方式:

1. **导出数据**: 在忘记密码前,定期导出数据备份
2. **清除扩展数据**: 
   - 右键点击扩展图标 → 管理扩展
   - 点击"删除扩展数据"
   - 注意:这会清除所有数据,包括使用记录

### 无法修改限制

检查以下几点:

1. 是否在冷却期内? → 等待冷却期结束
2. 是否达到每日上限? → 等到第二天
3. 密码是否正确? → 重新输入密码
4. 查看审计日志了解具体原因

### 安全功能不工作

1. 确认已启用"Enable Security Protection"
2. 确认已设置密码
3. 检查浏览器控制台是否有错误
4. 尝试重新加载扩展

## 未来改进

计划中的功能:

1. ✨ 密码恢复问题
2. ✨ 邮件通知(当检测到异常操作时)
3. ✨ 更详细的审计日志(包括IP地址、设备信息等)
4. ✨ 企业策略支持(防止卸载扩展)
5. ✨ 云同步(跨设备同步限制和日志)
6. ✨ 生物识别认证(指纹、面部识别)

## 技术支持

如有问题或建议,请:

1. 查看审计日志了解详细信息
2. 检查浏览器控制台的错误信息
3. 导出数据进行备份
4. 提交Issue到GitHub仓库

---

**版本**: 1.0.0  
**最后更新**: 2026-02-03
